(m + n) --> integer
2 --> integer
3 --> integer
ack --> (integer,integer) -> integer
ack --> (integer,integer) -> integer
ack(3, 2) --> integer
function ack(m, n) {
  return (m + n);
}
 --> (integer,integer) -> integer
m + n --> integer
m --> integer
m --> integer
n --> integer
n --> integer
x --> integer
